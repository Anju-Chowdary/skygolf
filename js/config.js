/**
 * SkyGolf — Central API config
 *
 * Change BASE to your production backend URL before deploying.
 * Example: "https://api.skygolf.in" or "https://skygolf-api.railway.app"
 *
 * This is the ONLY file you need to edit when switching from local → production.
 */
const BASE = "http://127.0.0.1:5000";

// Razorpay key — set your key_id here to enable online payments
// Leave empty ("") to disable online payments (users will be prompted to pay at venue)
const RAZORPAY_KEY = "";
